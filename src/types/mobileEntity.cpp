#include "mobileEntity.hpp"

/**
 * @brief       Constructor for a generic MobileEntity
 * 
 * @param hpBar_    Initialser for the overhead hpBar of each MobileEntity
 */
MobileEntity::MobileEntity(ProgressBar hpBar_) : hpBar(hpBar_) {}